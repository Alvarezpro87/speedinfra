// src/utils/email.ts
export async function sendContactEmail(data: { nome: string; email: string; mensagem: string }) {
    
    console.log('Enviando email com os dados:', data);
    
    return true;
  }
  